This wheelhouse contains the complete pinned Python 3.11 Linux x86_64 artifact
set listed in ../requirements.lock.json. Every wheel is hash- and size-bound;
offline installation must verify both before unpacking and must refuse any
missing, extra, incompatible, or unlisted artifact.
